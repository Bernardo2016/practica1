version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.x
    commands:
      - echo "Instalando dependencias..."
      - pip install boto3
  build:
    commands:
      - echo "Iniciando validación de registros desde DynamoDB..."
      - python validar_registros.py
